{{- if .Values.flower.enabled }}
{{- if not (eq .Values.flower.service.type "ClusterIP") }}
{{- fail "flower.service.type must be ClusterIP — Flower UI must not be exposed via NodePort or LoadBalancer. Use: kubectl port-forward svc/<release>-flower 5555:5555 -n <ns>" }}
{{- end }}
{{- /*
  Flower Service — ClusterIP only. Not in Ingress.
  Access via: kubectl port-forward svc/{{ include "vidya.componentName" (list . "flower") }} 5555:5555 -n <ns>
*/ -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "vidya.componentName" (list . "flower") }}
  labels:
    {{- include "vidya.componentLabels" (list . "flower") | nindent 4 }}
spec:
  type: {{ .Values.flower.service.type }}
  ports:
    - port: {{ .Values.flower.service.port }}
      targetPort: flower
      protocol: TCP
      name: flower
  selector:
    {{- include "vidya.componentSelectorLabels" (list . "flower") | nindent 4 }}
{{- end }}
